@extends('layouts.app')

@section('title', 'Browse Members — Thirumanam')

@section('content')
<div class="min-h-screen bg-gray-50">

  {{-- Page Header --}}
  <div class="bg-red-900 text-white py-8">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h1 class="text-2xl font-bold">Browse Members</h1>
      <p class="text-red-200 mt-1 text-sm">{{ number_format($total) }} profiles found</p>
    </div>
  </div>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <div class="flex flex-col lg:flex-row gap-6">

      {{-- Filter Sidebar --}}
      <aside class="w-full lg:w-64 flex-shrink-0" x-data="{ open: false }">

        <button @click="open = !open"
                class="lg:hidden w-full flex items-center justify-between bg-white border border-gray-200 rounded-lg px-4 py-3 text-sm font-medium text-gray-700 mb-4 shadow-sm">
          <span><i class="fas fa-filter mr-2 text-red-700"></i>Filters</span>
          <i class="fas fa-chevron-down transition-transform" :class="open ? 'rotate-180' : ''"></i>
        </button>

        <div class="bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden"
             :class="open ? 'block' : 'hidden lg:block'">
          <div class="px-4 py-3 bg-red-900 text-white">
            <h2 class="text-sm font-semibold uppercase tracking-wide">Filter Profiles</h2>
          </div>

          <form method="GET" action="{{ route('members.index') }}" class="p-4 space-y-5">

            <div>
              <label class="block text-xs font-semibold text-gray-500 uppercase mb-1">Search</label>
              <input type="text" name="search" value="{{ request('search') }}"
                     placeholder="Name or Member ID"
                     class="w-full border border-gray-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-red-500">
            </div>

            <div>
              <label class="block text-xs font-semibold text-gray-500 uppercase mb-1">Age Range</label>
              <div class="flex items-center gap-2">
                <input type="number" name="age_from" value="{{ request('age_from') }}"
                       placeholder="From" min="18" max="70"
                       class="w-full border border-gray-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-red-500">
                <span class="text-gray-400 text-xs">–</span>
                <input type="number" name="age_to" value="{{ request('age_to') }}"
                       placeholder="To" min="18" max="70"
                       class="w-full border border-gray-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-red-500">
              </div>
            </div>

            <div>
              <label class="block text-xs font-semibold text-gray-500 uppercase mb-1">Marital Status</label>
              <select name="marital_status"
                      class="w-full border border-gray-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-red-500">
                <option value="">Any</option>
                <option value="never_married" {{ request('marital_status') === 'never_married' ? 'selected' : '' }}>Never Married</option>
                <option value="divorced"       {{ request('marital_status') === 'divorced'       ? 'selected' : '' }}>Divorced</option>
                <option value="widowed"        {{ request('marital_status') === 'widowed'        ? 'selected' : '' }}>Widowed</option>
                <option value="separated"      {{ request('marital_status') === 'separated'      ? 'selected' : '' }}>Separated</option>
              </select>
            </div>

            <div>
              <label class="block text-xs font-semibold text-gray-500 uppercase mb-1">Education</label>
              <select name="education"
                      class="w-full border border-gray-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-red-500">
                <option value="">Any</option>
                <option value="10th"    {{ request('education') === '10th'    ? 'selected' : '' }}>10th</option>
                <option value="12th"    {{ request('education') === '12th'    ? 'selected' : '' }}>12th</option>
                <option value="diploma" {{ request('education') === 'diploma' ? 'selected' : '' }}>Diploma</option>
                <option value="ug"      {{ request('education') === 'ug'      ? 'selected' : '' }}>UG Degree</option>
                <option value="pg"      {{ request('education') === 'pg'      ? 'selected' : '' }}>PG Degree</option>
                <option value="phd"     {{ request('education') === 'phd'     ? 'selected' : '' }}>PhD</option>
              </select>
            </div>

            <div>
              <label class="block text-xs font-semibold text-gray-500 uppercase mb-1">City</label>
              <input type="text" name="city" value="{{ request('city') }}"
                     placeholder="e.g. Chennai"
                     class="w-full border border-gray-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-red-500">
            </div>

            <div>
              <label class="block text-xs font-semibold text-gray-500 uppercase mb-1">Star (Natchathiram)</label>
              <select name="star"
                      class="w-full border border-gray-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-red-500">
                <option value="">Any</option>
                @foreach(['Ashwini','Bharani','Krittika','Rohini','Mrigashira','Ardra','Punarvasu','Pushya','Ashlesha','Magha','Purva Phalguni','Uttara Phalguni','Hasta','Chitra','Swati','Vishakha','Anuradha','Jyeshtha','Mula','Purva Ashadha','Uttara Ashadha','Shravana','Dhanishtha','Shatabhisha','Purva Bhadrapada','Uttara Bhadrapada','Revati'] as $star)
                  <option value="{{ $star }}" {{ request('star') === $star ? 'selected' : '' }}>{{ $star }}</option>
                @endforeach
              </select>
            </div>

            <div>
              <label class="block text-xs font-semibold text-gray-500 uppercase mb-1">Rasi</label>
              <select name="rasi"
                      class="w-full border border-gray-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-red-500">
                <option value="">Any</option>
                @foreach(['Mesha','Rishabha','Mithuna','Kataka','Simha','Kanya','Tula','Vrischika','Dhanus','Makara','Kumbha','Meena'] as $rasi)
                  <option value="{{ $rasi }}" {{ request('rasi') === $rasi ? 'selected' : '' }}>{{ $rasi }}</option>
                @endforeach
              </select>
            </div>

            <div class="pt-2 flex gap-2">
              <button type="submit"
                      class="flex-1 bg-red-800 hover:bg-red-900 text-white text-sm font-medium py-2 rounded-lg transition">
                Apply
              </button>
              <a href="{{ route('members.index') }}"
                 class="flex-1 text-center bg-gray-100 hover:bg-gray-200 text-gray-700 text-sm font-medium py-2 rounded-lg transition">
                Reset
              </a>
            </div>

          </form>
        </div>
      </aside>

      {{-- Results Grid --}}
      <main class="flex-1">

        @if(count($members) === 0)
          <div class="bg-white rounded-xl shadow-sm border border-gray-100 p-16 text-center">
            <i class="fas fa-users text-4xl text-gray-300 mb-4"></i>
            <p class="text-gray-500 font-medium">No profiles found</p>
            <p class="text-gray-400 text-sm mt-1">Try adjusting your filters</p>
          </div>
        @else

          <div class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-5">
            @foreach($members as $member)
              @php
                $profile  = $member['profile'] ?? [];
                $photo    = $profile['profile_photo'] ?? null;
                $photoUrl = $photo
                  ? 'https://api.thirumanam.info/storage/' . $photo
                  : null;
                $name     = $member['name']           ?? 'Member';
                $memberId = $member['member_no']       ?? '—';
                $age      = $profile['age']            ?? null;
                $city     = $profile['city']           ?? null;
                $edu      = $profile['education']      ?? null;
                $marital  = $profile['marital_status'] ?? null;
                $userId   = $member['id']              ?? null;
              @endphp

              <div class="bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden hover:shadow-md transition group">

                <div class="relative overflow-hidden h-52 bg-gray-100">
                  <img src="{{ $photoUrl ?? 'https://ui-avatars.com/api/?name=' . urlencode($name) . '&background=8B1A1A&color=fff&size=200' }}"
                       alt="{{ $name }}"
                       loading="lazy"
                       class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
                       onerror="this.src='https://ui-avatars.com/api/?name={{ urlencode($name) }}&background=8B1A1A&color=fff&size=200'">
                  <span class="absolute top-2 left-2 bg-black/60 text-white text-xs px-2 py-0.5 rounded-full">
                    {{ $memberId }}
                  </span>
                </div>

                <div class="p-4">
                  <h3 class="font-semibold text-gray-900 text-base truncate">{{ $name }}</h3>
                  <div class="flex items-center gap-3 text-sm text-gray-500 mt-1">
                    @if($age) <span><i class="fas fa-birthday-cake text-xs mr-1"></i>{{ $age }} yrs</span> @endif
                    @if($city) <span><i class="fas fa-map-marker-alt text-xs mr-1"></i>{{ $city }}</span> @endif
                  </div>
                  @if($edu || $marital)
                    <div class="flex flex-wrap gap-1 mt-2">
                      @if($edu)
                        <span class="bg-gray-100 text-gray-600 text-xs px-2 py-0.5 rounded-full">{{ ucfirst($edu) }}</span>
                      @endif
                      @if($marital)
                        <span class="bg-red-50 text-red-800 text-xs px-2 py-0.5 rounded-full">{{ ucwords(str_replace('_', ' ', $marital)) }}</span>
                      @endif
                    </div>
                  @endif

                  <div class="flex gap-2 mt-4">
                    <a href="/members/{{ $userId }}"
                       class="flex-1 text-center text-sm font-medium bg-red-800 hover:bg-red-900 text-white py-2 rounded-lg transition">
                      View Profile
                    </a>
                    <button onclick="sendInterest({{ $userId }}, {{ $member['profile_id'] ?? 0 }})"
                            class="flex-1 text-sm font-medium border border-pink-400 text-pink-500 hover:bg-pink-50 py-2 rounded-lg transition">
                      <i class="fas fa-heart mr-1"></i> Interest
                    </button>
                  </div>
                </div>

              </div>
            @endforeach
          </div>

          {{-- Pagination --}}
          @if($lastPage > 1)
            <div class="mt-8 flex items-center justify-center gap-1 flex-wrap">
              @if($currentPage > 1)
                <a href="{{ request()->fullUrlWithQuery(['page' => $currentPage - 1]) }}"
                   class="px-3 py-2 text-sm rounded-lg border border-gray-200 hover:bg-gray-50 text-gray-600">
                  <i class="fas fa-chevron-left text-xs"></i>
                </a>
              @endif

              @for($p = max(1, $currentPage - 2); $p <= min($lastPage, $currentPage + 2); $p++)
                <a href="{{ request()->fullUrlWithQuery(['page' => $p]) }}"
                   class="px-3 py-2 text-sm rounded-lg border transition
                          {{ $p === $currentPage ? 'bg-red-800 border-red-800 text-white font-semibold' : 'border-gray-200 hover:bg-gray-50 text-gray-600' }}">
                  {{ $p }}
                </a>
              @endfor

              @if($currentPage < $lastPage)
                <a href="{{ request()->fullUrlWithQuery(['page' => $currentPage + 1]) }}"
                   class="px-3 py-2 text-sm rounded-lg border border-gray-200 hover:bg-gray-50 text-gray-600">
                  <i class="fas fa-chevron-right text-xs"></i>
                </a>
              @endif
            </div>
            <p class="text-center text-xs text-gray-400 mt-3">
              Page {{ $currentPage }} of {{ $lastPage }} &middot; {{ number_format($total) }} profiles
            </p>
          @endif

        @endif
      </main>

    </div>
  </div>
</div>

{{-- Toast notification --}}
<div id="interest-toast"
     class="fixed bottom-6 right-6 bg-gray-900 text-white text-sm px-4 py-3 rounded-xl shadow-lg hidden z-50 transition">
  <span id="interest-toast-msg"></span>
</div>

<script>
function sendInterest(memberId, profileId) {
  fetch('/members/' + memberId + '/interest', {
    method: 'POST',
    headers: {
      'X-CSRF-TOKEN': document.querySelector('meta[name="csrf-token"]').getAttribute('content'),
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({ profile_id: profileId })
  })
  .then(r => r.json())
  .then(data => showToast(data.message ?? 'Interest sent!'))
  .catch(() => showToast('Something went wrong. Please try again.'));
}

function showToast(msg) {
  const toast = document.getElementById('interest-toast');
  document.getElementById('interest-toast-msg').textContent = msg;
  toast.classList.remove('hidden');
  setTimeout(() => toast.classList.add('hidden'), 3000);
}
</script>
@endsection
